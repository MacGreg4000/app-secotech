{
  "name": "openbtp-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Serveur MCP pour OpenBTP — proxy fin vers /api/agent/*",
  "type": "module",
  "bin": { "openbtp-mcp": "dist/index.js" },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
